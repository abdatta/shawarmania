# Tasks — hero-experience

- [x] 1.1 `components/Header/` — sticky blur header, exact logo, anchors, Order Now pill,
      hide/show on scroll direction.
- [x] 1.2 `components/FloatingCtas/` — call + WhatsApp round buttons from brand.json.
- [x] 1.3 `components/Loader/` — logo pop loader, fonts.ready + 2.5s cap, ScrollTrigger.refresh.
- [x] 2.1 `sections/Hero/` — night-poster composition (type left, neon card right), entrance
      timeline (back.out pops, sticker stamp, card tilt-in), light scroll parallax, reduced-motion
      composed state, mobile stack.
- [x] 2.2 `sections/Marquee/` — seamless dual-copy loop, Lenis-velocity timeScale, static under
      reduced motion.
- [x] 3.1 App recomposition (Loader/Header/Hero/Marquee/FloatingCtas); retire Foundation section.
- [x] 4.1 Self-review: build green; screenshots at 3 viewports (static + live-scroll steps);
      entrance timing check; reduced-motion shot fully composed; no horizontal overflow; header
      hide/reveal verified in live mode; no console errors. Iterate until it looks like a
      fast-food brand poster, then archive.
