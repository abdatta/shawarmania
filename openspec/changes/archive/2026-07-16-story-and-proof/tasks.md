# Tasks — story-and-proof

- [x] 1.1 Register SplitText in lib/gsap.ts.
- [x] 1.2 `sections/Story/` — SplitText headline, narrative, timeline rail + drawn connector,
      Bengali background accent.
- [x] 2.1 `components/Counter/` — one-shot number tween, en-IN formatting, reduced-motion final.
- [x] 2.2 `sections/Proof/` — counter band + lab-certified nutrition module (label card,
      safety checks, certificate polaroid).
- [x] 3.1 `sections/Testimonials/` — vlogger cards + quote cards, staggered entrance.
- [x] 4.1 App composition; self-review shots (3 viewports, static + live), counter landing values
      verified in DOM, timeline data check, reduced-motion pass, overflow check; archive.
