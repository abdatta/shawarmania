# Tasks — franchise-funnel

- [x] 1.1 `sections/Franchise/` — momentum header, tier cards, support row, process rail,
      FAQ accordion, enquiry banner + form (degrading states).
- [x] 2.1 App composition before Footer; `#franchise` anchor live.
- [x] 3.1 Self-review: shots (3 viewports); null-economics rendering; FAQ keyboard toggle;
      CTA fallback logic (tel since whatsappNumber null); disabled form note; test filled state
      by temporarily setting portal values then reverting; overflow; archive.
